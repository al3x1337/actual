---
category: Fixes
authors: [al3x1337]
---

Budget Views now seed their category and group ordering from the global layout whenever a view is created or recreated, and they clean up stale per-view ordering data when categories or groups are removed. This ensures every view keeps its own ordering instead of falling back to the default list whenever the global order changes.
